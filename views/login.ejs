<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome Page</title>
    <style>
      body {
        margin: 0;
        font-family: Arial;
      }
      header {
        width: 100%;
        height: 70px;
        background: #eb324a;
        display: flex;
        align-items: center;
      }
      nav {
        padding: 10px;
        background-color: #ffadb8;
        margin: 5px;
        color: white;
        border-radius: 5px;
        transition: all 350ms;
        cursor: pointer;
      }

      nav:hover {
        transform: scale(1.05);
      }

      main {
        margin-left: 50px;
      }

      a {
        text-decoration: none;
        color: white;
      }

      button {
        padding: 15px;
        background-color: #eb324a;
        border: none;
        border-radius: 5px;
        transition: all 350ms;
      }

      button:hover {
        transform: scale(1.05);
      }
    </style>
  </head>
  <body>
    <header>
      <nav><a href="/auth/logout">LogOut</a></nav>
      <nav><a href="/auth/login">Login</a></nav>
      <nav><a href="/">Welcome</a></nav>
    </header>
    <main>
      <h1>This is a Login Page</h1>
      <h2>Please login here...!</h2>
      <p>You are going to feel the atmosphere of Excellabs.</p>

      <button><a href="/auth/google">Google OAuth2.0</a></button>
    </main>
  </body>
</html>
